name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  # Placeholder: Replace with actual tests once stack is chosen.
  # Example for Python:
  # test:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-python@v5
  #       with:
  #         python-version: '3.12'
  #     - run: pip install -r requirements.txt
  #     - run: pytest

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder lint check
        run: echo "Lint step defined after stack decision"
