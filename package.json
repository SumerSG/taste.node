{
  "name": "taste-node-web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd web && npm install --include=dev && npm run build",
    "start": "cd web && npx serve -s dist -l $PORT"
  }
}
